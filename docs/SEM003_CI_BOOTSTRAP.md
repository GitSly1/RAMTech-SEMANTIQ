# SEM-003 CI Bootstrap

This temporary bootstrap change places the SEMANTIQ pull-request workflow on the default-branch path so GitHub can evaluate subsequent SEM-003 pull-request commits with the repository CI definition.

Scope is limited to CI activation. It does not alter product source or the protected RTUDES baseline.
